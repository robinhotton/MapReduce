# imports
import sys

# ouvrir le flux standard (terminal)
input = sys.stdin

# Autre méthode pour récuperer les données plus efficacement dans le sys.stdin

# boucler sur les données
for line in input:

    # extraire uniquement les champs souhaités
    
    # filtrer les champs
    
    # afficher les champs
    print(line)