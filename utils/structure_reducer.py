# imports
import sys

# OPTIONNEL: variables pour stocker les données

# ouvrir le flux standard (terminal)
input = sys.stdin

# Autre méthode pour récuperer les données plus efficacement dans le sys.stdin

# boucler sur les données
for line in input:

    # récupérer les données
    
    # Aggréger les données
    
    # OPTIONNEL: afficher ou stocker les données
    print(line)

# OPTIONNEL: reste de la logique du code pour traiter et exporter les données
